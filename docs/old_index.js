// Libraries
import Papa from 'papaparse';

// Read csv

// Use jQuery to read and import csv

/* broken D:
const dataset = Papa.parse('../Project_Database.csv', {
    delimiter: ",",
    download: true,
    header: true,
    complete: function(results) {
        console.table(results.data);
        }
    }
);

*/